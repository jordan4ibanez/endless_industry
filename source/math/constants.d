module math.constants;

import std.math.constants;

static immutable double QUARTER_PI = PI * 0.25;
static immutable double DIV_QUARTER_PI = 1 / QUARTER_PI;
static immutable double HALF_PI = PI * 0.5;
